import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  loader: {
    display: "flex",
    height: "80vh",
    width: "80vw",
    justifyContent: "center",
    alignItems: "center",
  },
});
