import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  selectCustomer: {
    // width: "20%",
    // marginRight: "1rem",
    position: "relative",
  },
  metaItems: {
    height: "100%",
  },
  currentBalance: {
    position: "absolute",
    bottom: -22,
    left: 45,
  },
});
