import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  selectCustomer: {
    width: "20%",
    marginRight: "1rem",
  },
  metaItems: {
    marginRight: "1rem",
    marginLeft: "1rem",
    height: "100%",
  },
});
