import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  drawer: {
    height: "60% !important",
    width: "45vw",
    top: "calc( 50% - 30% ) !important",
    borderRadius: "0px 6px 6px 0px",
    padding: `1.2rem`,
  },
});
