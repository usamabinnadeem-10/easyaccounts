import React from "react";

import { useHistory } from "react-router";

import { Grid } from "@mui/material";

const ExternalCheques = () => {
  return <Grid container>ExternalCheques</Grid>;
};

export default ExternalCheques;
